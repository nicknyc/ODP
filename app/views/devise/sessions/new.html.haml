%body.login-page
  .login-box
    .login-logo
      %a{:href => "/"}
        %p ODP
    / /.login-logo
    .login-box-body
      .login-box-msg
        %p เข้าสู่ระบบ
        %small{:style => " text-align: center;"} *ด้วยอีเมลล์,รหัสบัตรประชาชน หรือ ไอดี
      = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        .form-group.has-feedback
          = f.text_field :login, class: 'form-control', autofocus: true
          %span.glyphicon.glyphicon-envelope.form-control-feedback
        .form-group.has-feedback
          = f.password_field :password, class: 'form-control', autocomplete: 'off'
          %span.glyphicon.glyphicon-lock.form-control-feedback
        .row
          .col-xs-8
            .checkbox.icheck
              %label
                =link_to "สมัครสมาชิก", new_user_registration_path
          / /.col
          .col-xs-4
            = f.submit 'เข้าสู่ระบบ', class: 'btn btn-primary btn-block btn-flat'
          / /.col
    / /.login-box-body
  / /.login-box
  = javascript_include_tag 'application', 'data-turbolinks-track' => true
